# Ergo 快速启动指南

## 🚀 启动服务

### 方法 1：一键启动（推荐）

双击运行 `start-ergo.bat` 即可启动所有服务：

```bash
start-ergo.bat
```

该脚本会自动启动：
1. **API Bridge** (端口 8082) - 后端 API 服务
2. **自动配对监听器** - OpenClaw 设备自动审批
3. **前端服务器** (端口 8081) - 静态页面 + API 代理

### 方法 2：分别启动

如果需要分别启动服务：

```bash
# 终端 1: 启动 API Bridge
npm run api

# 终端 2: 启动前端服务器
npm start

# 终端 3: 启动自动配对（可选）
npm run pairing
```

---

## 📍 访问地址

- **本地开发**: http://localhost:8081
- **公网访问**: https://terryzin.cpolar.cn
- **API 端点**: http://localhost:8082/api/*

---

## ❌ 常见问题

### 1. 页面显示"连接被拒绝"

**原因**: API Bridge 服务器未运行

**解决方案**: 运行 `start-ergo.bat` 或 `npm run api`

### 2. 定时任务信息丢失

**原因**: API Bridge 未连接到 OpenClaw Gateway

**解决方案**:
1. 确保 OpenClaw Gateway 正在运行（端口 18789）
2. 检查 `server/api-bridge.js` 中的配置

### 3. 项目管理页面无数据

**原因**: API Bridge 未运行

**解决方案**: 运行 `start-ergo.bat`

---

## 🔍 故障排查

### 检查服务状态

```bash
# 检查端口占用
netstat -ano | findstr :8081
netstat -ano | findstr :8082

# 检查 OpenClaw Gateway
netstat -ano | findstr :18789
```

### 查看日志

API Bridge 会在控制台输出详细日志：
- 连接状态
- API 请求
- 错误信息

---

## 📝 开发模式

### 运行测试

```bash
# 运行所有测试
npm test

# 仅测试本地
npm run test:local

# 仅测试公网
npm run test:public
```

### 调试模式

在 `server/api-bridge.js` 启动时会显示：
- 服务端口
- WebSocket 状态
- 认证状态
- 文件监听路径

---

## 🔐 认证配置

默认 API 密钥: `ergo-default-secret-key-2026`

修改密钥：
1. 访问 http://localhost:8081/settings.html
2. 输入新密钥
3. 保存

或设置环境变量：
```bash
set ERGO_SECRET=your-custom-key
start-ergo.bat
```

---

## 📊 v1.5 新功能

- ✅ WebSocket 实时推送
- ✅ 浏览器通知
- ✅ 一键触发 Cron 任务
- ✅ 多项目仪表盘

查看详细更新: [CHANGELOG.md](CHANGELOG.md)

---

**需要帮助？** 查看完整文档: [README.md](README.md)
